# go/

Future home of the Go rewrite of `nt`. Empty for now.

The Go implementation will be a from-scratch port that satisfies the same
contracts as the Python v1 — see [`docs/`](../docs/) for the binding specs
(CLI contract, daemon protocol, storage format). It is explicitly **not** a
translation of the Python source; the Go author reads the contracts, not the
Python code.